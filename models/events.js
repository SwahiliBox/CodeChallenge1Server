/*var mongoose = require("mongoose");

var Event = mongoose.Schema({

		eventId: {
			type:Number,
			required:true
		},

		eventTitle: {
			type:String,
			default:"",
			required:true
		},

		eventDescription: {
			type:String,
			default:"",
			required:true

		},

		eventVenue: {
			type:String,
			default:"",
			required:true
		}

		eventstartDate: {
			type:Date,
			required:true
		},

		eventendDate: {
			type:Date,
			required:true
		}

});

module.exports = mongoose.model("Event", Event);*/